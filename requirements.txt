# Core dependencies for Accelerating Dual Momentum Investing application
Flask>=2.3.0
pandas>=2.0.0
yfinance>=0.2.0

# Flask dependencies (automatically installed but pinned for stability)
Werkzeug>=2.3.0
Jinja2>=3.1.0
MarkupSafe>=2.1.0
itsdangerous>=2.1.0

# Pandas dependencies (automatically installed but pinned for stability)  
numpy>=1.24.0
python-dateutil>=2.8.2
pytz>=2023.3
